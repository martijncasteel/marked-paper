docker container kill $(docker container ls -a -q)
docker container rm $(docker container ls -a -q)

docker build -t martijncasteel/marked-paper .
docker run -p 3000:80 -d martijncasteel/marked-paper
